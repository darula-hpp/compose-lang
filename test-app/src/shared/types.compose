## Shared data types ##

define structure User
  has id as number
  has name as text
  has email as text
  has createdAt as text

define structure Product
  has id as number
  has name as text
  has price as number
  has details as text
